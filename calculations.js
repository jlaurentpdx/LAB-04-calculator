// eslint-disable-next-line no-unused-vars
export function add(x, y) {
    //return x + y;
}

// export function subtract(x, y) {return x - y;}

// export function multiply(x, y) {return x * y;}

// export function divide(x, y) {return x / y;}

// export function modulo(x, y) {return x % y;}

// export function intDivide(x, y) {return Number(x / y).toFixed(0);}

// export function exponent(x, y) {return x ** y;}

// export function pythagResult(x, y) {return (x * x) + (y * y);}

// export function pythagFindC(x, y) {return Number(Math.sqrt(pythagResult(x, y)).toFixed(2));}
